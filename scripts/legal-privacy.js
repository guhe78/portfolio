/**
 * Initializes the legal privacy page by setting up language options, footer, and mobile menu toggle functionality.
 * This function is called when the legal privacy page is loaded to ensure that the language settings, footer, and mobile menu are properly initialized.
 */
function initLegalPrivacy() {
  initLanguage();
  initFooter();
  toggleMobileMenu();
}
